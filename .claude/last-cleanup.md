2026-07-31 — portfolio-scoped cleanup pass (jlmwines + portfolio root; VaadAi/AliyahNet skipped per lower-cadence convention), triggered by a /review-claude finding recurring structure/status/doc-hygiene drift.

Touched this pass:
- `~/.claude/hooks/status-hygiene-guard.js` — generalized from a single hardcoded target (`jlmwines/plans/STATUS.md`) to a `TARGET_FILES` list so it also governs `projects/STATUS.md` (portfolio dashboard), which previously had zero write-time drift protection despite already being 15 days stale with wrong deploy numbers for two active projects. Portfolio file gets its own zone map (Health table ≈ Metrics, Business/Marketing/Websites/Technology bullets ≈ Current State) and is exempted from the jlmwines-only version-consistency check and the Updated-line marker-count cap (a multi-project summary legitimately cites one marker per project). Verified via pipe-tests: real portfolio content passes clean, an injected `@NNN-@NNN` range gets blocked, jlmwines behavior unchanged (including correctly still flagging the violation below).
- `jlmwines/plans/STATUS.md` — "At a glance" jlmops bullet was 219 chars (over the hook's own 200-char cap, confirmed by pipe-testing the live file against the updated hook) with a dated clause; collapsed to current-state prose. `Last Active` metric was 3 days behind the file's own `Updated:` date (2026-07-27 vs. 2026-07-30); corrected.
- `projects/STATUS.md` (portfolio) — fully refreshed: `Updated:` date, Health table (JLM Wines jlmops @498→@544, VaadAi v0.20.3 @216→v0.28.0 @228, both Last Active dates), and the Business/Marketing/Technology/Portfolio-Notes bullets for both projects, which were 15 days stale.
- `CALENDAR.md` (portfolio root) — `Updated:` date bumped; JLM Wines content-pipeline row was stale (still listed a shipped post as "in pipeline," didn't mention the now-active region-post series) — corrected against `jlmwines/plans/STATUS.md`. Other `Today/This Week` rows not independently re-verified this pass (no contradicting evidence found, left as-is).
- `.claude/bugs.md` — two resolved entries (2026-07-26 WebXltM, 2026-07-24 code-audit sequence) had grown into full root-cause paragraphs (300-1400 chars) instead of symptom + pointer; collapsed to one line each, pointing at `.claude/session-log.md` / `jlmops/plans/CODE_AUDIT_FIX_SEQUENCE.md` for the detail.
- `.claude/session-log.md` — pruned entries from 2026-06-23 through 2026-06-30 (now 31+ days old), extending the prior pass's boundary from "before 2026-06-23" to "before 2026-07-01." Found while pruning: `UNIFIED_TASK_UI_PLAN.md`, referenced repeatedly in that range as "closed," no longer exists anywhere in the repo (not `plans/`, not `_archive/`) — noted in the pruned-block summary, not otherwise actioned (its facts are already reflected in `ARCHITECTURE.md`).
- `jlmops/plans/MANAGER_UI_PLAN.md` — self-declared "All phases implemented and live" (top-level Status) but was sitting unarchived in `plans/`. Confirmed via grep that its core facts (`ManagerDashboardView`/role-based UI) are already documented in `jlmops/docs/ARCHITECTURE.md`, and that nothing else in the repo links to the plan by path (only referenced by name in `UI_AUDIT.md`, which already tolerates naming archived plans). Archived via `git mv` to `_archive/`.

Audited, no change needed:
- `.claude/bugs.md` open jlmops items dated since the 2026-07-23 pass (2026-07-24 Admin Dashboard Tasks card, ManagerDashboard Projects card, doGet authorization; 2026-07-22 Woo API push attributes; 2026-07-20 Correct Product Name confirmation) — cross-checked against current `plans/STATUS.md`, all still genuinely open and accurately described.
- `plans/STATUS.md` Inbox — 1 deferred item (offline-attribution, `defer:2026-08-10`), not yet due.
- Memory (`~/.claude/projects/.../memory/`, jlmwines + vaadai + portfolio) — spot-checked against this session's findings and current STATUS files, no stale entries or contradictions found.

Out of scope this pass (VaadAi/AliyahNet lower-cadence per portfolio kernel "Project Cadence & Scrutiny"; no cleanup marker exists for either): no sweep of `vaadai/plans/`, `vaadai/.claude/`, or AliyahNet docs.
