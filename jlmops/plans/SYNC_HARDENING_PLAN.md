# Sync Widget Hardening Plan

**Created:** 2026-05-05
**Status (current, 2026-08-26):** Six named bugs. **Bug 4** — resolved: the stuck-job reaper + 2026-08-25 auto-retry/error-surfacing hardening covers the stuck-spinner symptom; the remaining gap (retry landing on a dead-end spinner stage for `IMPORTING_PRODUCTS`/`EXPORTING_ORDERS`/`VALIDATING` failures — `PUSHING_WEB_INVENTORY`/`IMPORTING_COMAX` were already handled) is fixed in the working tree, see `retryFailedStepBackend` below. **Bugs 1-3** — no code-level fix possible without a live staging repro (confirmed independently in May and re-confirmed 2026-08-26); cross-cutting hardening committed to build regardless. **Bug 5** (cross-execution sync-state race) and **Bug 6** (validation ERROR silently treated as pass) — both fully designed and independently reviewed, ready to build. Full detail per bug below.
**Scope:** Daily Sync widget UI/state drift. Excludes failed-Comax-import recovery (rare; tracked separately in `.claude/bugs.md`).

## Implementation log

- **2026-05-05** — Implemented Bug 4 fix #1 in `OrchestratorService.js`:
  - New private helper `_reapStuckJobInSession(jobType, sessionId, thresholdMinutes)` — finds the matching `PROCESSING` job for the active session, marks it `FAILED` if `processed_timestamp` is older than the threshold, sends a `NotificationService.reportFailure` matching the existing zombie-killer pattern.
  - Called from each of the three async branches in `_checkAndAdvanceSyncState` (`IMPORTING_COMAX`, `VALIDATING`, `GENERATING_WEB_EXPORT`) before the `getJobStatusInSession` call. So a stuck job is reaped on the next poll, not on the next hourly trigger.
  - Threshold: 8 minutes. Apps Script's hard execution limit is 6 min — anything stuck past 8 is dead by definition. Tighter than the existing 15-min hourly zombie killer because polls run continuously.
  - Existing 15-min zombie killer in `processPendingJobs` left in place — still useful for non-sync jobs and as a safety net.
  - Outcome: stuck spinner caps at ~8 min instead of "up to 60 min". After reap, normal `FAILED` branch handles state transition + UI Retry button.

## Goal

Eliminate the 4 frequent race / stale-state issues in the Daily Sync widget so users don't see stuck buttons, missing stages, or buttons that fire too early. Make the UI a faithful mirror of the backend state at all times.

## Scope

In:

1. Generate web export button visible/clickable before the action can fire (orig 2026-01-28)
2. Export button stays visible after export step starts (orig 2025-12-29)
3. Sync widget doesn't show Comax product import stage when order export is skipped without a refresh (orig 2025-12-31)
4. Generate button stays after export completes — file generated and named, but button doesn't reset (orig 2026-03-17, distinct from 2026-03-03 stale-poll fix)

Out (deferred):

- Failed Comax import can't recover when corrected file is uploaded — rare case, separate plan.

## Architecture recap

- **Backend state** lives in one JSON object in SysConfig at `system.sync.state`, managed by `SyncStateService.js`. 12 stages + FAILED. Strict transition table at `SyncStateService.js:35-50`. Every backend action has a stage guard.
- **Frontend** is `AdminDailySyncWidget_v2.html` (single file, ~636 lines). One source of truth for the shared message/action area: `STAGE_CONFIG[stage]` lookup table at lines 164-179, rendered by `updateSharedArea()` at lines 236-281.
- **Polling** runs at 1s during spinner stages, 10s during waiting stages, off when IDLE/COMPLETE. See `adjustPolling()` at lines 576-606.
- **Action lifecycle** in `runAction()` at lines 385-458: disable buttons → spinner → backend call → success handler updates UI from returned state.
- **Race protection** today:
  - `actionInProgress` flag (line 157) — polls during an action skip the shared-area update (lines 542-557).
  - Strict-less-than stale-poll discard (lines 560-564) — the 2026-03-03 fix.

## Backend audit (read on 2026-05-05)

Confirmed by reading `WebAppSync.js` and `OrchestratorService.js`:

- **All user-triggered backend functions correctly transition AND return the post-state.** Specifically:
  - `importWebOrdersBackend` (`WebAppSync.js:123-190`): when `ordersToExportCount === 0`, sets stage = `WAITING_COMAX_IMPORT`, marks step3 'skipped', step4 'waiting', returns the post-state.
  - `exportComaxOrdersBackend` (`WebAppSync.js:201-264`): transitions through `EXPORTING_ORDERS` → `WAITING_ORDER_CONFIRM` (or `WAITING_COMAX_IMPORT` if empty), returns post-state.
  - `generateWebExportBackend` (`WebAppSync.js:389-430`): transitions to `GENERATING_WEB_EXPORT`, queues a job via `OrchestratorService.queueWebInventoryExport`, calls `OrchestratorService.run('hourly')`, returns. **Final transition out of GENERATING_WEB_EXPORT happens asynchronously via `_checkAndAdvanceSyncState`** (not by the backend function itself).

- **Polling endpoint `getSyncStateFromBackend` (`WebAppSync.js:17-23`) calls `OrchestratorService.checkAndAdvanceSyncState()` on every poll.** That means polling actively drives async stage advancement — the UI is the engine.

- **`_checkAndAdvanceSyncState` GENERATING_WEB_EXPORT branch (`OrchestratorService.js:1175-1230`) advances ONLY on terminal job statuses:**
  - `jobStatus === 'COMPLETED'` → `WAITING_WEB_CONFIRM` (or `COMPLETE` if no changes).
  - `jobStatus === 'FAILED' || 'QUARANTINED'` → `FAILED`.
  - **Any other status (`PENDING`, `PROCESSING`, `NOT_FOUND`, `ERROR`) → no transition.** Stage hangs at `GENERATING_WEB_EXPORT`. Spinner stays.

- **Job lifecycle (`OrchestratorService.js:621-680`):** queue creates `PENDING` row. `processPendingJobs` sets `PROCESSING` before calling the service. **Service is responsible for setting `COMPLETED`** itself — orchestrator only sets `FAILED` if the service throws.

- **Zombie killer exists (`OrchestratorService.js:567-601`):** any job stuck in `PROCESSING` for >15 minutes is marked `FAILED`. **But the zombie killer only runs inside `processPendingJobs`**, which is only called from `OrchestratorService.run(...)`. **Polling does NOT call `processPendingJobs`** — it only calls `_checkAndAdvanceSyncState`. So between hourly triggers, zombies aren't reaped.

- **`retryFailedStepBackend` (`WebAppSync.js:512-535`) sets stage = `failedAtStage` directly via `setSyncState` (not `transition`).** Doesn't validate. Doesn't re-queue jobs. So a failed async job + Retry only re-checks the job status; the export is not actually re-run.

## Diagnosis per bug

### Bugs 1-4 — re-audited against current code, 2026-08-26

Diagnosis below is from 2026-05-05, written against a since-removed stage (`GENERATING_WEB_EXPORT`, replaced 2026-05-12 by the synchronous export + async `PUSHING_WEB_INVENTORY` push). Re-verified via `git log`/`git diff` what's shipped since, per stage:

- **Bug 4 — resolved.** The stuck-job reaper (fix candidate #1, shipped 2026-05-05) already covers `PUSHING_WEB_INVENTORY`, the stage that replaced `GENERATING_WEB_EXPORT` (`OrchestratorService.js`, confirmed in Bug 5's own audit above). 2026-08-25 added auto-retry of still-failing rows before giving up, real per-SKU error surfacing, and fixed the "permanent polling freeze after any error" display bug. **Fix candidate #4, re-examined 2026-08-26:** `retryFailedStepBackend` (`WebAppSync.js:633-680`) doesn't literally re-queue a job for any stage — but for `PUSHING_WEB_INVENTORY`/`IMPORTING_COMAX` it was already redirecting to a `WAITING_*` stage with a real button (`WAITING_WEB_CONFIRM`/`WAITING_COMAX_IMPORT`), which achieves the same outcome: clicking that button queues a fresh job cleanly. This wasn't a gap for those two — it was already correct, and the plan's earlier framing of it as unresolved was wrong. **The actual gap:** three *other* `failedAtStage` values that can reach the default branch — `IMPORTING_PRODUCTS`, `EXPORTING_ORDERS`, `VALIDATING` — are pure spinner stages (`STAGE_CONFIG` `button: null`, confirmed for all three), so the default branch's "redirect to the same stage" sends the user into a dead end with no button and no queued job, recoverable only via Reset. Fixed in the working tree: `IMPORTING_PRODUCTS` → `IDLE`; `EXPORTING_ORDERS` → `WAITING_ORDER_EXPORT`; `VALIDATING` → `WAITING_COMAX_IMPORT` (validation runs as a job chained after Comax import, so re-running the import re-queues both). `IMPORTING_ORDERS` and `WAITING_WEB_EXPORT` are the only other stages that can appear in `failedAtStage`, and both already have real buttons in `STAGE_CONFIG`, so the default branch is correct for them as-is.
- **Bugs 1-3 — code has NOT changed at their relevant stages since May.** Confirmed via `git diff` across every commit touching `AdminDailySyncWidget_v2.html` since: `WAITING_ORDER_EXPORT`, `WAITING_COMAX_IMPORT`, `EXPORTING_ORDERS`, `IMPORTING_ORDERS` and their surrounding poll/action-guard logic are untouched by both the 2026-05-12 refactor and the 2026-08-25 hardening pass. **The May diagnosis's own conclusion stands: none of these three can get a confirmed root-cause fix without a live staging repro** — that diagnosis was written with full knowledge of the current `actionInProgress`/stale-poll-discard architecture (still current, re-confirmed 2026-08-26: `runAction` at `:377-441` shows the spinner immediately on click before the backend call resolves, and the success handler calls `updateUI(status)` directly with the returned state rather than waiting on the next poll — both mechanisms already rule out the most obvious race explanations, which is exactly why the diagnosis called these "reframed" rather than closed). Guessing a fix without repro is the "no guessing" this whole plan exists to avoid.
- **What IS buildable now, without a repro, for all three:** the cross-cutting hardening items below (spec'd 2026-05-05, never built) close the whole class of display-lag race regardless of which exact mechanism is firing, and add the instrumentation needed to nail the exact cause the next time any of the three actually reproduces. Committing to build these now rather than leaving them as an indefinitely-deferred "someday" list.

### Bug 4: Generate button stays after export completes

**Confirmed root cause (cause #1):** the export job can finish its work (file written to Drive) but its row in SysJobQueue never reaches `COMPLETED`, leaving stage stuck at `GENERATING_WEB_EXPORT`.

How this happens:

1. User clicks Generate. `generateWebExportBackend` queues a `PENDING` job, calls `OrchestratorService.run('hourly')`, returns.
2. `processPendingJobs` picks up the job, sets `PROCESSING`, calls `ProductImportService.processJob(...)` (or whichever service runs the export).
3. Service writes the file to Drive successfully.
4. Service is about to write `COMPLETED` to the queue row, but Apps Script's 6-minute hard timeout fires first. Or service throws AFTER file write but before status update. Or any other path where the status update is skipped.
5. Status stays `PROCESSING`. File exists on Drive. Stage stuck at `GENERATING_WEB_EXPORT`.
6. Widget polls. `_checkAndAdvanceSyncState` reads job status = `PROCESSING`, doesn't advance. Spinner forever (or whatever the UI shows when stage = GENERATING_WEB_EXPORT).
7. **Zombie killer would catch this — but only when `processPendingJobs` runs, not on polls.** Until the next hourly trigger fires `OrchestratorService.run('hourly')`, the zombie sits.

This matches the symptom: "file is generated and named, but button doesn't reset". The user sees the file in Drive but the widget never advances.

The "Generate button" framing in the original report is likely loose terminology — the user means "the action area never returns to a clickable state" (still showing spinner from `GENERATING_WEB_EXPORT`). After up to 1 hour, the next hourly trigger reaps the zombie → status FAILED → widget shows Retry. User clicks Retry → stage returns to `GENERATING_WEB_EXPORT`, polls re-check the (now FAILED) status → widget shows Retry again. So even after the zombie kill, the widget cycles between Failed and Retry — never returning to Generate.

**Cause #2 (less likely but worth noting):** the same stuck-status pattern can happen earlier in the lifecycle (job stuck in `PENDING` because `processPendingJobs` didn't pick it up). Symptom would be identical — spinner stays.

**Fix candidates (in order of value):**

1. **Inline zombie check in `_checkAndAdvanceSyncState`** for the specific job being polled. If status is `PROCESSING` and `processed_timestamp` > N minutes old, mark `FAILED` right there. Catches stuck jobs on every poll, not just hourly. Lowest risk, highest value.
2. **Tighter zombie threshold for export jobs** — 6 minutes (Apps Script execution limit) instead of 15 minutes. The export can't legitimately exceed 6 minutes.
3. **Cross-check stage by file existence** — if `webExportFilename` is set on the state and the file exists in Drive, force-advance to `WAITING_WEB_CONFIRM` regardless of job status. Recovers from the file-written-but-status-not-set timing window without waiting for zombie kill.
4. **Make `retryFailedStepBackend` re-queue the failed job**, not just reset the stage — so retry actually re-runs the export instead of re-checking the same FAILED status.

### Bug 3: Comax import stage hidden after order export skip

**Reframed after backend audit.** `importWebOrdersBackend` correctly transitions to `WAITING_COMAX_IMPORT` and returns the post-state with `step3 = 'skipped'`, `step4 = 'waiting'`. The widget's `updateUI(status)` should render the "Import Comax" button immediately via `STAGE_CONFIG[WAITING_COMAX_IMPORT]`. **Backend is not the bug.**

Likely causes:

1. **A poll fires between the click and the success handler return** (10s idle interval, but action could take seconds). During the action, `actionInProgress = true` blocks shared-area updates. After the success handler returns, `actionInProgress = false`. The next poll reads stage = `WAITING_COMAX_IMPORT` and renders correctly. So the user *should* see the button. Unless the success handler itself isn't rendering — possible if `runAction` exited early.
2. **The user "without a refresh" description may mean within a brief invisible window.** Hard to confirm without repro.

**Verification:** repro on staging with a sync that has 0 orders to export. Click Import Orders. Open console, log every `updateSharedArea` call with `(stage, source)`. Determine whether the stage actually arrives in the success handler.

**Fix candidate:** TBD until repro. If the success handler does receive `WAITING_COMAX_IMPORT` and still doesn't render, the bug is in `updateSharedArea` or a CSS/DOM issue.

### Bug 2: Export button stays visible after export step starts

**Reframed after backend audit.** `exportComaxOrdersBackend` synchronously transitions through `EXPORTING_ORDERS` → `WAITING_ORDER_CONFIRM` and returns the post-state. So the success handler should never see stage = `WAITING_ORDER_EXPORT` post-click.

Plausible cause: a polling race during the action. `runAction` sets the spinner immediately. Polls during the action are blocked from updating the shared area. So the spinner should hold. UNLESS:

- The user clicked Export but the backend call hadn't yet started (network queue, Apps Script cold start). Polling fires, returns stage = `WAITING_ORDER_EXPORT`. But `actionInProgress` is true, so the poll skips the shared area. So the spinner from `runAction` stays.
- After the success handler runs and `actionInProgress = false`, the next poll fetches a fresh state. If the backend has fully advanced past `EXPORTING_ORDERS`, the new button (Confirm or Import Comax) renders. So no Export button.

**Most likely real cause:** the symptom may be misdescribed. The user may be seeing a brief moment during the action where the spinner doesn't replace the button cleanly (CSS layout shift, or the `disabled` state visible). Need staging repro to characterize.

**Verification:** record screen during a successful export-with-orders sync. Capture pixel-by-pixel what shows in the shared action area from click to confirm-button render.

### Bug 1: Generate button live too early

**Status:** still unclear without staging repro. The button only shows for stage = `WAITING_WEB_EXPORT`, reached after `VALIDATING` completes. If clicking it "doesn't fire", the backend stage guard at `WebAppSync.js:395` would throw with the actual stage in the message — visible in browser console.

**Verification:** repro on staging. When the bug fires, check browser console for the thrown error. The error message will reveal what stage the backend was actually in.

**Fix candidate:** TBD until repro. If it's a stage-guard rejection, the question is why the widget showed the button when stage wasn't `WAITING_WEB_EXPORT` — likely a polling display race we need to characterize.

## Cross-cutting hardening

After fixing individual bugs, harden against the whole class of races:

1. **Stuck-stage detector.** The async pattern (`GENERATING_WEB_EXPORT` waiting on a job to flip to `COMPLETED`) has no timeout. If `_checkAndAdvanceSyncState` finds stage = `GENERATING_WEB_EXPORT` for > N minutes (say 10), advance to `FAILED` with a "stuck" error message. Same pattern likely applies to other async stages (`IMPORTING_PRODUCTS`, `IMPORTING_COMAX`, `VALIDATING`).
2. **Retry actually retries.** `retryFailedStepBackend` currently just resets the stage. For async-job stages, it should also re-queue the job. For synchronous-action stages, resetting the stage and letting the user click again is fine.
3. **Render gate in widget.** Shared message/action area is updated only by (a) the success handler from a fresh action, or (b) a poll whose `status.lastUpdated` is strictly newer than the last applied state. Track `lastAppliedTimestamp` separate from `lastActionTimestamp`.
4. **Stage assertion in success handler.** When an action expects specific post-transition stages, assert. Log a warning when the returned stage is unexpected — surfaces backend regressions immediately.
5. **Button-enable invariant.** Whenever a button is rendered into `#sharedAction`, set `disabled` based on `actionInProgress`. Today the disable is only on click-time (line 388); if a poll re-renders during an action, the new buttons are enabled by default.
6. **Single transition log.** Dev-only console log (`SyncWidget: stage <prev> → <new> via <source>`) so future race bugs are diagnosable from the browser console without instrumentation.

## Fix sequence

Order fixes by what we can land without staging repro vs. what needs repro first:

1. **Stuck-stage detector** (cross-cutting #1) — defensible to add even without repro; unblocks the most painful symptom (forever spinner) regardless of whether it's the actual root cause of Bug 4.
2. **Bug 4 staging repro** with browser console capture of polled `status.stage` and `status.webExportFilename`. Determines whether the cause is stuck-job-status or retry-doesn't-retry. Fix follows from observation.
3. **Bug 1, 2, 3 staging repro** — backend looks correct for all three; the bugs are widget-side or in the async job system. Each needs targeted observation before a fix.
4. **Render gate + button-enable invariant** (cross-cutting #3 and #5) — apply once individual root causes are confirmed, so the hardening doesn't mask which specific fix worked.
5. **Retry-actually-retries** (cross-cutting #2) — only worth doing if Bug 4 cause #2 is confirmed.

## Verification protocol

Staging repro for each bug — must produce before/after evidence:

- **Bug 4:** start a sync, advance to `WAITING_WEB_EXPORT`, click Generate, observe whether "Generate" button reappears post-completion. Browser console open the whole time.
- **Bug 2:** start a sync with orders to export, advance to `WAITING_ORDER_EXPORT`, click Export, observe whether Export button visibly persists during the export.
- **Bug 3:** start a sync with zero orders to export. Click Import Orders. Observe whether "Import Comax" appears without manual refresh.
- **Bug 1:** advance to `WAITING_WEB_EXPORT` and click Generate as fast as the button appears. Observe whether the click fails, and capture the error.

Each fix lands as its own commit so we can isolate cause/effect on staging.

## Open questions

- ~~For Bug 4: how does `getJobStatusInSession` actually evaluate?~~ **Answered 2026-05-05.** Returns one of `PENDING`/`PROCESSING`/`COMPLETED`/`QUARANTINED`/`FAILED`/`NOT_FOUND`/`ERROR`. `_checkAndAdvanceSyncState` only handles the terminal three; the others leave stage stuck. Zombie killer reaps stuck PROCESSING after 15 min but only via `processPendingJobs`, not via polls — explains hour-long stuck-spinner symptom.
- Should the render-gate use `lastAppliedTimestamp` (track applied state) or `expectedStageAfterAction` (track what we're waiting for)? The latter is stricter but harder to maintain.
- Should the inline zombie check (Bug 4 fix #1) apply to ALL `*ING_*` stages or just `GENERATING_WEB_EXPORT`? Same pattern likely affects `IMPORTING_PRODUCTS`, `IMPORTING_COMAX`, `VALIDATING`. Low cost to handle uniformly.
- Add a dev-mode console-log toggle to the widget for race diagnosis without code edits?

## Bug 5: Concurrent sync-state race — duplicate log entries (confirmed 2026-08-26, plan rewritten after independent review)

**First draft of this section (2026-08-26, superseded below) proposed a standalone lock-plus-trim fix.** An independent review found it incomplete in ways that would have repeated the 2026-08-25 fix's failure mode (looked reasonable, didn't close the gap) — see corrections below. Do not build the original three-point fix as first written; build the plan under "Corrected fix" instead.

**Confirmed via live log** (`exchange/JLMops_Logs - SysLog.csv`): this is not a one-off. Duplicate "Advancing to WAITING_WEB_EXPORT" rows from distinct execution IDs appear on 8/19, 8/20, 8/21, 8/23, 8/24, and 8/26. Worst observed: 8/24, 06:21:06–06:21:17, **twelve** distinct `INTERNAL-<uuid>` executions advancing the same session within an 11-second window (2026-08-26's own burst was five, at 06:47:57–06:48:04). Confirmed real — twelve *separate* Apps Script executions, not one execution logging repeatedly. **The 2026-08-25 log-dedup fix in `SyncStateService.setSyncState` does not help**: it only skips logging when one execution's own read-then-write sees no stage change; with no lock, overlapping executions each read the pre-transition stage before any of them writes back, so each legitimately sees a real transition from its own vantage point.

**This exact race was already diagnosed and staged 2026-06-14 — and never shipped.** `jlmops/plans/RELIABILITY_AUDIT.md` §1.3 documents an identical incident that same day (three concurrent runners — contexts `085635c1`/`e5142428`/`4918c516` — each advancing the same session within 5 seconds), traces it to the same non-idempotent, unlocked `_checkAndAdvanceSyncState`/`setSyncState`, and designs a 5-stage `LockService` rollout (`LockHelpers.withScriptLock` helper, then locking `setSyncState`/`processPendingJobs`/`purgeOldJobs`/`pullOrders` one at a time with 24h+ observation between each). **Confirmed nothing from that design ever shipped** — `LockHelpers`, `withScriptLock`, and `SyncStateService.mutateSyncState` don't exist anywhere in the repo (grepped). §1.3's own diagnosis already states the key correctness point the first draft of this section missed: *locking the write alone is not sufficient* — the read-modify-write must happen **inside** the lock (its proposed `mutateSyncState(fn)` re-reads state after acquiring the lock, applies the mutation, then writes), and the duplicate advances need an idempotency guard (no-op if the session is already at/past the target stage), not just mutual exclusion. §1.4 (a related, smaller fix — decide "changes vs. no changes" from `exportWebInventory`'s return value, not a re-read of clobberable state) **has already shipped** — confirmed live in `WebAppSync.js#generateWebExportBackend`, which reads `result.changed` and reports a `sync.web_export.state_clobber` failure if the re-read state disagrees. Only §1.3 itself (the lock/idempotency work) is outstanding.

**Call sites (corrected — the first draft missed the dominant one).** `_checkAndAdvanceSyncState()`/`processPendingJobs()` are reachable from: the real hourly Apps Script trigger (`runHourlyTrigger`); `WebAppSync.js`'s Push-Web-Inventory action (`OrchestratorService.run('hourly')` inline on a user click); `finalizeJobCompletion`'s inline `processPendingJobs()` call after any job completes; the recursive call inside `_checkAndAdvanceSyncState`'s own IMPORTING_COMAX branch (same execution, not a concurrency source, but worth a comment so it isn't mistaken for one); and — missed in the first draft — **`WebAppSync.js#getSyncStateFromBackend`, which calls `OrchestratorService.checkAndAdvanceSyncState()` on every widget poll**, at 1-second cadence during spinner stages (`AdminDailySyncWidget_v2.html`'s own polling design, already noted in this doc's May section above: "the UI is the engine"). Burst sizes of 5-12 concurrent executions line up with 1s polling far better than with any of the other four paths — this is almost certainly the dominant driver, and it must be in scope for locking, not left out.

**`HousekeepingService.js#performFrequentMaintenance` already guards against running during an active sync** (lines 596-603, shipped 2026-05-15 per `FREQUENT_PIPELINE_PLAN.md` — confirmed live, reads `SyncStateService.getActiveSession()` and skips if stage isn't IDLE/COMPLETE/FAILED). The first draft's fix #3 proposed adding this guard again and misattributed which function runs log cleanup/validation/Mailchimp pulls (that's `performDailyMaintenance`, a separate daily-cadence function, not this one) — drop that fix entirely, there's nothing to build here.

### Fix, finalized (2026-08-26, 3rd pass — exhaustive write-site migration)

**Supersedes the "corrected fix" this section held earlier today.** That version (lock only `_checkAndAdvanceSyncState`'s 3 branches + `exportWebInventory`) is itself incomplete: a full-repo audit (`grep -rn "SyncStateService\.\(setSyncState\|transition\|updateStep\)("`) found 15+ more writers of the same `system.sync.state` blob left unlocked, including `OrchestratorService.js#finalizeJobCompletion` (writes `archiveFileIds` mid-session, unlocked, confirmed at line ~913) and nearly every `WebAppSync.js` `*Backend` action function. **Locking a subset does not close the race** — an unmigrated writer still reads a stale unlocked snapshot and can clobber, or be clobbered by, a migrated writer's fresh locked state. A partial migration isn't a lower-risk version of this fix, it's a different and still-incomplete one — exactly the failure mode already seen twice (2026-08-25, first 2026-08-26 draft). Every write to `system.sync.state` must go through one locked primitive; no curated subset.

**Stage A — unchanged from §1.3.** Ship `LockHelpers.js#withScriptLock(context, timeoutMs, fn)` exactly as specced (`tryLock`, 30s timeout, log `lock-contention` + return `null` on contention, always release in `finally`). Smoke test; confirm `doGet`/`doPost` stay responsive while the lock is held. **Wait 24+ hours before Stage B** — this window de-risks the LockService mechanism itself (zero prior codebase precedent), not the migration.

**Stage B — exhaustive migration, shipped as one deploy (not split further — see rationale below).**
1. Add `SyncStateService.mutateSyncState(fn)`: acquire `withScriptLock`, re-read state fresh, call `fn(state)` to mutate in place, `setSyncState(state)`, release. **`fn`'s contract (reconciled with point 4 below, per 6th independent review, 2026-08-26 — the two were finalized separately and never explicitly tied together): `fn` mutates the fresh `state` object in place and returns normally to apply it, or throws `SyncStageStaleError` to signal "the guard I depend on no longer holds, abort."** `mutateSyncState` never catches that throw — it propagates to the caller (after releasing the lock). Its sibling `mutateSyncStateBestEffort(fn)` uses the exact same `fn` contract, but catches `SyncStageStaleError` specifically, logs it, and returns `{applied: false, state: <the fresh, unwritten read>}` instead of propagating — any OTHER exception `fn` throws (a genuine bug, not an expected staleness) propagates from both variants unchanged, never swallowed. On success, both variants return `{applied: true, state: <the written state>}` (`mutateSyncState` can be treated as always `applied: true` when it returns at all, since it throws instead of returning `false`). On lock contention (`withScriptLock` returns `null`): `mutateSyncState` throws a **dedicated `SyncLockContentionError`** — deliberately a different type from `SyncStageStaleError`, not a plain `Error` (7th independent review, 2026-08-26: a plain error here was indistinguishable from "a genuine failure" at the call site, causing contention to either spuriously mark a healthy sync FAILED or, if the recovery write also hit contention, silently swallow the error and reintroduce exactly the "click did nothing" symptom this whole primitive exists to prevent). **Its human-readable message ("The sync system is busy — please wait a moment and try again.") is set once, at construction, inside `mutateSyncState` itself — not left for a catch block to add later (9th independent review: found the message was only specified at a catch-block re-throw in point 4, so a first-write site with no surrounding `try`/`catch` at all — `skipToComaxBackend`, currently dead code with zero live callers, unflagged as such until now — would let a message-less error propagate raw to the frontend).** Every caller, whether it catches and re-throws or lets it propagate untouched, gets the same clear message for free. `mutateSyncStateBestEffort` returns `{applied: false, state: <a plain unlocked getSyncState() read>}` on contention, same as a guard mismatch — never throws. **`fn` must mutate the passed-in `state` object's own properties (`state.foo = x`) — never reassign `state` itself to a new object.** Any call site that today builds a brand-new object via `SyncStateService.getDefaultState()` — e.g. `const newState = SyncStateService.getDefaultState(); newState.sessionId = ...;` — rather than mutating a previously-read state falls into this trap if migrated literally: reassigning the local `state` parameter inside `fn` never touches the object `mutateSyncState` actually persists, silently no-op'ing the write with no error at all. **Check every call site of `getDefaultState()`, not a hand-picked subset (10th independent review, 2026-08-26: the 8th pass named only `importWebProductsBackend` and `apiPullAllBackend`, missing `SyncStateService.js#resetSyncState` — `const defaultState = getDefaultState(); setSyncState(defaultState);`, the same shape, reachable through `resetSyncStateBackend`, which point 4 below already commits to migrating).** `getDefaultState()` has exactly 5 call sites repo-wide (grep-confirmed) — the three that feed a write (`importWebProductsBackend`, `apiPullAllBackend`, `resetSyncState`) all migrate the same way: copy `getDefaultState()`'s fields ONTO the existing `state` reference (`Object.assign(state, SyncStateService.getDefaultState(), {...any per-call overrides})`), never declare a new local variable.
2. Re-implement `transition()` and `updateStep()` on top of `mutateSyncState` internally — fixes their call sites (`WebAppSync.js:907`, the `WooProductPullService.js`/`WebAppSync.js:138` `updateStep` calls) without touching each one.
3. Migrate every remaining raw `getSyncState()` → mutate → `setSyncState()` pair to `mutateSyncState(fn)`, same mutation logic, new wrapper. Confirmed sites (2026-08-26 audit):
   - `OrchestratorService.js` — `_checkAndAdvanceSyncState`'s three branches (IMPORTING_COMAX, VALIDATING, PUSHING_WEB_INVENTORY) and `finalizeJobCompletion`'s `archiveFileIds` write — the site the narrower plan missed.
   - `WebAppSync.js` — every `*Backend` function that writes sync state: `importWebProductsBackend`, `importWebOrdersBackend`, `exportComaxOrdersBackend`, `confirmComaxUpdateBackend`, `startComaxImportBackend`, `generateWebExportBackend`, `pushWebInventoryBackend`, `confirmWebInventoryUpdateBackend`, `retryFailedStepBackend`, `resetSyncStateBackend`, `updateSyncStateFromOrchestrator`, `skipToComaxBackend`, `apiPullAllBackend`.
   - `ProductService.js#exportWebInventory` — both the no-changes and has-changes `webExportFilename` writes.
   - `WooProductPullService.js#pullAndImportAll` — 7 `updateStep()` calls across its multi-phase pipeline (`WooProductPullService.js:321,332,343,354,364,368,376`; EN/HE product staging, validation, orders), reachable only via `apiPullAllBackend`. Not a separate write site to hand-migrate — covered automatically once `updateStep()` itself routes through `mutateSyncState` (point 2). Listed for the audit record, since it isn't visible from grepping `WebAppSync.js` alone.
   - `updateSyncStateFromOrchestrator` (`WebAppSync.js:763`) has zero live call sites anywhere in the repo (confirmed via grep, including all `.html`) — dead code. Harmless to migrate along with everything else; not worth further audit effort.
4. **Guard rule — one principle plus two mechanics needed to make it unambiguous (a 5th independent review, 2026-08-26, found the 4th pass's principle sound in outline but underspecified in two spots: (a) a function's `catch` block can be entered EITHER because write 1's own mismatch-check threw, OR because some unrelated real error happened after write 1 succeeded — the plan's prose only gave an example of the second case and generalized "every write after the first, including catch blocks" without saying how to tell the two apart, so a literal reading would wrongly no-op a genuine write-1 rejection instead of surfacing it; (b) `mutateSyncState` is built on `withScriptLock`, whose `fn` never runs at all on lock contention — the plan never said what a write-1 caller sees on contention, so under real polling load (which the plan itself says causes frequent contention) a genuine button-click could silently do nothing instead of throwing or succeeding. Both close below with explicit mechanics, not more prose.)**
   - **The governing distinction is still "does this write change `state.stage`,"** unchanged since the 3rd pass: a write that only touches other fields (`updateStep()`, `exportWebInventory`'s `webExportFilename`, `finalizeJobCompletion`'s `archiveFileIds`) needs locking only — re-read-inside-lock plus mutate-only-your-own-field means any concurrent `.stage` change already in the fresh read survives untouched. Confirmed correct by 3 independent reviews now — unchanged.
   - **Two `mutateSyncState` variants, chosen by whether "couldn't acquire the lock" and "guard already stale" should mean the same thing to the caller:**
     - **`mutateSyncState(fn)`** — for the very first stage-changing write in a function (`expectedCurrentStage` = the entry-guard stage). On lock contention, **throws** `SyncLockContentionError` (does not silently return). Inside the lock, if `state.stage !== expectedCurrentStage`, throws a dedicated `SyncStageStaleError` (not a generic `Error`) — a typed marker so the calling function's `catch` block can tell "I lost a race to a legitimate concurrent process," "the lock was simply busy," and "something actually broke" apart from each other. All three outcomes surface identifiably to the calling `catch` block, rather than a single generic throw the plan's own recovery logic could otherwise misclassify.
     - **`mutateSyncStateBestEffort(fn)`** — for every write after the first (a success-path "write 2," a catch-block write once write 1 is confirmed done, `_checkAndAdvanceSyncState`'s branches, and every non-stage-changing write). On lock contention OR a fresh-read guard mismatch, **silently no-applies** — logs a warning, returns the current true state, does not throw. Contention here means a concurrent process is already actively mutating this exact state; standing down is the same correct response as a mismatch, not a new case.
   - **`expectedCurrentStage` is reassigned, not fixed, across a function's execution — stated explicitly here because two prior reviews each found an unstated variant of this exact ambiguity:** it starts as the entry-guard stage for write 1. **The instant write 1's `mutateSyncState` call returns successfully, `expectedCurrentStage` is reassigned to the stage write 1 just set** (e.g. `exportComaxOrdersBackend`: starts `WAITING_ORDER_EXPORT`, becomes `EXPORTING_ORDERS` right after write 1 succeeds). Write 2 (and any catch-block recovery write) then compares the fresh read against THIS updated value, never the original entry stage — comparing against the original would make write 2 report stale on every normal, uncontended call, since write 1 already, correctly, moved the stage away from it.
   - **Catch-block disambiguation — three outcomes, not two, since a 7th independent review found "genuine failure" and "the lock was simply busy" were being conflated:** each function tracks one boolean, `write1Done`, set `true` only after its first `mutateSyncState(fn)` call returns without throwing. Its `catch (e)` block then does exactly one of three things — never a blind write:
     - **`e instanceof SyncStageStaleError`** (write 1 itself was rejected — by definition `write1Done` is still `false`): do **not** write anything. Log at info/warn ("lost the race to a concurrent process before this action began — no failure to record") and `return SyncStateService.getSyncState()`. A guard-mismatch is not a failure of *this* sync, so it must never produce a `FAILED` write — writing one here would itself be the clobber the whole fix exists to prevent.
     - **`e instanceof SyncLockContentionError`** (the lock itself was busy — this can happen whether or not `write1Done`, since the recovery write below acquires the lock too): do **not** write anything and do **not** treat it as a sync failure — nothing was learned about whether the action would have succeeded. Log at warn and re-throw the same `SyncLockContentionError` as-is — its message is already set at construction (point 1), not something this catch block needs to add. **Correction, 8th independent review:** `AdminDailySyncWidget_v2.html`'s `runAction()` failure handler (`:442-455`) has no type-based branching and no toast mechanism — it renders any thrown error identically, as red "Failed: `{err.message}`" text. There is no differentiated visual/semantic outcome here, and this plan doesn't add one (that would be a frontend-scope change). What actually differs for the user is the **message text alone** — "busy, please try again" instead of a stack-trace-flavored failure string — surfaced through the exact same generic rendering path a real failure uses. This is a smaller improvement than "distinct retryable outcome" implied, but an honest one: still strictly better than today's silent lost-update, not the richer UX the previous pass overstated.
     - **Any other error** (a genuine failure — network, code, data): write `FAILED` via `mutateSyncState(fn)` if `!write1Done` (acting as this execution's own first stage-changing write — re-checks the original entry guard, throws `SyncStageStaleError`/`SyncLockContentionError` per the same rules rather than double-failing something already resolved) or via `mutateSyncStateBestEffort(fn)` if `write1Done` (acting as a later write — checks the current `expectedCurrentStage`, no-applies silently on mismatch or contention). **This recovery write is itself wrapped in its own try/catch:** if it throws `SyncStageStaleError` or `SyncLockContentionError`, treat exactly as the two bullets above (no `FAILED` write, no spurious failure); if it throws anything else, `logger.error` both the original failure and this second one, and still `return SyncStateService.getSyncState()` — a `*Backend` function must never let an exception escape uncaught to the frontend, even when recording the failure itself fails. Follows the general `applied:false` → `reportFailure` rule below.
   - **General rule, and a completeness check keyed on the primitive's call site, not on the string `'FAILED'` (10th independent review, 2026-08-26: found two problems with the 9th pass's version — (a) grepping for the literal string `stage = 'FAILED'` has a real blind spot, `SyncStateService.js#transition()`, which sets `state.stage = newStage` with `newStage` as a variable, invisible to a content-based grep exactly the way `rebuildSysConfigFromSource`'s variable-key write defeated point 5's original gate; (b) the rule's "zero exceptions" framing collides with `_checkAndAdvanceSyncState`'s branches below, which correctly use the OPPOSITE polarity — this needed an explicit carve-out, not an implicit one a checker has to already know about):**
     - **Scope, precisely:** this rule applies to every `WebAppSync.js` `*Backend` function's `mutateSyncStateBestEffort` calls that write `stage = 'FAILED'` after a service call reports failure without throwing — check `applied`, and if `false`, fire `NotificationService.reportFailure` (`WebAppSync.js:428-434` precedent), because the write failing to apply means a real failure has nowhere else to surface. Known instances, illustrative only: `startComaxImportBackend:353-362`, `importWebProductsBackend:78-86`, `apiPullAllBackend:1008-1014`, `importWebOrdersBackend:143-151`, `exportComaxOrdersBackend:242-250`.
     - **Explicit carve-out, not an exception a checker has to infer:** `_checkAndAdvanceSyncState`'s three branches also call `mutateSyncStateBestEffort` for `FAILED` writes, but use the **opposite** polarity by design — gate `reportFailure` on `applied === true`, per point 205 below, because these are idempotent background pollers where only the execution that actually won the race should notify (the "genuine failure has nowhere else to surface" reasoning above doesn't apply — the failure DID get recorded, by whichever poller won). This rule and that one are deliberately different; a checker must treat `OrchestratorService.js#_checkAndAdvanceSyncState` as governed by point 205, not this bullet.
     - **The completeness check searches for calls to the primitive itself, `mutateSyncStateBestEffort(`, not for the string `'FAILED'`** — a fixed function name introduced by this migration, not spoofable via a variable the way the write's target value is. Post-migration, every live call site of `mutateSyncStateBestEffort(` (a finite, enumerable set, unlike "every place `stage` might end up `'FAILED'`") gets its `fn` body read to determine whether it can set `stage = 'FAILED'` (literal or via a variable/constant such as `STAGES.FAILED`) and, if so, which polarity applies per the two bullets above. This is what actually closes the `transition()` blind spot: `transition()` is unaffected regardless, because point 2 always routes it through `mutateSyncState` (the throwing variant) — never `mutateSyncStateBestEffort` — so it's outside this rule's domain entirely, not a gap inside it. **Accepted latent risk, noted not fixed (11th independent review, 2026-08-26):** this check is sound only as long as no shared helper is later introduced that itself wraps `mutateSyncStateBestEffort` (the way `transition()`/`updateStep()` wrap `mutateSyncState`) — such a helper would reintroduce the same indirection blind spot one level removed. No such helper exists today and this plan doesn't add one; a future refactor that does must re-run this completeness check against the new indirection, not just re-grep the old pattern.
   - **`_checkAndAdvanceSyncState`'s three branches** use `mutateSyncStateBestEffort` with their own idempotency check (no-op if the session is already at/past the target stage) for the state write itself — unchanged since the 1st draft, still what stops Bug 5's duplicate "State saved" log line. **Every side effect inside all three branches — not only the `reportFailure` calls — fires only when the write's result has `applied: true`.** Named explicitly, so a future pass doesn't have to re-derive the list: the three FAILED-path `NotificationService.reportFailure`/`_createFailureTask` calls (`OrchestratorService.js:1207-1213`, `:1234-1240`, `:1307-1313`); the IMPORTING_COMAX success path's `finalizeSync(state.sessionId)` + `processPendingJobs()` (`:1201-1202` — omitted from the prior pass's list, 7th independent review); and the PUSHING_WEB_INVENTORY success path's `TaskService.completeTaskByTypeAndEntity` + `_registerSessionFilesFromOrchestrator` (`:1277-1296`). The VALIDATING branch's success path has no side effects beyond its own write, so nothing further to gate there. Rationale unchanged: these currently sit unconditionally alongside the write, not gated by whether this particular execution actually won the idempotency check — multiple concurrent pollers observing the same terminal job status could otherwise each run these for what's really one underlying event. Bounded in most cases by each callee's own dedup (`_createFailureTask`'s `entityId` check, `finalizeSync`'s pending-job check), but gating on `applied` closes it at the source instead of relying on every downstream callee to independently self-guard.
   - **`skipToComaxBackend`** (`SyncStateService.transition()`'s only caller, `WebAppSync.js:907`) has zero live callers anywhere in the frontend (confirmed via grep, 9th independent review) — dead code, same status as `updateSyncStateFromOrchestrator` (point 3). Harmless to migrate along with everything else: its only write is a "first write" — `transition()` reimplemented on `mutateSyncState` (not best-effort) per point 2, throwing `SyncStageStaleError` via the same TRANSITIONS-table check on mismatch. It has no surrounding `try`/`catch`, which is fine now that `SyncLockContentionError`'s message is set at construction (point 1) rather than at a catch-block re-throw.
   - **`resetSyncStateBackend`**: "always allowed" by design — unconditional locked overwrite via `mutateSyncState` (no guard condition, so contention is the only throw case; correct as-is, admin override should fail loudly if it can't acquire the lock rather than silently no-op).
5. **Completeness gate — grep patterns plus one structural hole, closed without a full-function lock.** Post-migration, three literal patterns must return zero matches outside `SyncStateService.js`: `grep -rn "SyncStateService.setSyncState(" jlmops/*.js`, `grep -rn "syncStateService.setSyncState(" jlmops/*.js` (lowercase alias, `SyncStateService.js:275`), `grep -rn "ConfigService.setConfig(['\"]system\.sync\.state" jlmops/*.js` (direct bypass). **A structural 4th path no grep can ever match:** `generate-config.js`'s `rebuildSysConfigFromSource` template writes `system.sync.state` via the variable `rk.name` inside its generic `RUNTIME_KEYS` snapshot/restore loop (`SetupConfig.js:33,91`) — snapshotting all 10 runtime-mutable keys before wiping the whole SysConfig sheet, restoring them after. Live, routine deploy-flow step (`jlmops/CLAUDE.md`: "clasp push → rebuildSysConfigFromSource()"); a real write landing in the clear→restore window is silently clobbered by the stale pre-clear snapshot, for all 10 keys.
   - **Two fixes already tried and rejected, both confirmed wrong by independent review:** (1) compare `lastUpdated` timestamps — broken because `getSyncState()` auto-stamps `lastUpdated` to "now" on any read that finds none (`SyncStateService.js:70-72`), so live always looks newer, skipping the restore almost every time. (2) wrap the whole function (snapshot through restore, including `sheet.clear()` + `setValues()` over `getMasterConfiguration()`'s ~1700-row array) in one `withScriptLock` — this directly violates the plan's own cited locking discipline, `RELIABILITY_AUDIT.md` CCP-4: "scope to critical section only — never the full function (would burn 6-min budget inside the lock and starve other handlers including `doGet`/`doPost`)." A multi-second-to-double-digit-second script-wide lock during a routine deploy would queue or contend every migrated writer and every 1s widget poll for that whole window — the exact thing CCP-4 exists to prevent.
   - **Correct fix — a short lock around a structural identity check, not a timestamp or a full-function lock:** `getMasterConfiguration()`'s literal default `json` value for the `system.sync.state` row is a fixed, known constant (`config/system.json`, confirmed `"{}"`; the other 9 `RUNTIME_KEYS` all default to `""`, also confirmed) — read via `ConfigService.getConfig(name)[key]` directly (a genuinely raw, undefaulted read, already what the existing snapshot loop at `SetupConfig.js:55-56` uses — no `SyncStateService.getSyncState()` stage-aware defaulting involved). Right before restoring each `RUNTIME_KEYS` entry (inside the existing per-key loop, `SetupConfig.js:87-97`), acquire the lock briefly, re-read the CURRENT live value for that key with that same raw read, and compare it to that key's own known masterConfig default: if the live value still equals the default, nothing has written since the clear — safe to restore the snapshot. If it does **not** equal the default, something wrote real state during the window — skip the restore for that key (log a warning; leave the live write in place). No real writer ever produces either sentinel value (`"{}"` or `""`), so there's no false-positive "looks safe but wasn't" case. **The compare and the restore write happen inside the same lock hold, never released between them** (10th independent review: stated explicitly because a check-then-release-then-write implementation would reopen the exact TOCTOU window this fix exists to close, and would contradict §1.3's own lesson that a read-modify-write must happen *inside* the lock, not just have its write locked). Ten short, independent lock acquisitions (one per key, each a single read-compare-write) is the accepted cost, folded into the contention-volume accounting below, not a new full-function hold.
   - **Residual, explicitly accepted exposure — two narrower gaps than the one this closes, not silently ignored:** `sheet.clear()` + `setValues(masterConfig)` (`SetupConfig.js:70-75`, unlocked, ~1700 rows) resets the live cell to its default for however long the rewrite takes, before the restore loop above even starts. Any concurrent *reader* landing in that window (a `*Backend` function's guard check, or a fresh read inside a `mutateSyncState`/`mutateSyncStateBestEffort` call) can see a transient, spuriously-IDLE `system.sync.state` — bounded, self-correcting once the restore loop runs moments later, and never a silent overwrite (worst case is a spurious guard rejection, surfaced the same way any other guard rejection is). Locking the rewrite itself would violate CCP-4 exactly as the rejected full-function-lock fix did; this window is accepted as the cost of complying with it. **Second, narrower case (11th independent review):** if a real writer's locked write lands in that same unlocked window and wins the key's restore-loop slot before the loop reaches `system.sync.state`, the structural-identity check correctly sees a live value that isn't the default and skips the restore — but that also means the *pre-deploy* session's snapshot is discarded outright, not deferred or merged, a real (if narrow — requires the write to land in a multi-row-write's brief unlocked window, on a deploy day, during an active sync) loss of whichever in-progress session state the snapshot held. Accepted for the same reason as the CCP-4 tradeoff above: recovering it would need either locking the rewrite (rejected) or a merge strategy more complex than this fix's scope — a deploy during an active sync session is already an operational anti-pattern the runbook should discourage, not a case this fix needs to make lossless.

**Why Stage B isn't split further:** all these writers share one JSON blob, so a partial migration leaves the race open for every unmigrated site — it doesn't reduce blast radius the way Stages A/C/D/E do for genuinely separate subsystems (helper mechanism; job purge; job pickup; order pull). Splitting the migration itself just ships known-incomplete fixes on a delay. **Lock-contention volume under aggregate load, considered explicitly:** once all ~20 sites are live, acquisition frequency is high (1s polling during spinner stages, up to 8 sequential acquisitions inside `apiPullAllBackend`'s pipeline, and 10 more during a `rebuildSysConfigFromSource` deploy) — but every critical section is a single JSON read+write with no long-running work inside the lock (per `withScriptLock`'s own design, §1.3), and the great majority of writes now use the best-effort variant that treats contention as an expected no-op rather than an error, so contention should manifest as brief queuing/silent skip, not starvation or user-visible failure. This is a claim, not yet a measurement — Smoke B (below) must observe actual acquisition/contention rates under a real sync, not just the isolated two-call smoke from Stage A.

**Confirmed already in scope, no extra work:** `WebAppSync.js#getSyncStateFromBackend`'s poll-triggered `checkAndAdvanceSyncState()` call (1s cadence during spinner stages, confirmed in `AdminDailySyncWidget_v2.html#adjustPolling`) goes through the same locked path automatically once `_checkAndAdvanceSyncState`'s internals are migrated.

**Confirmed correctly dropped:** the "collapse `runHourlyTrigger`'s redundant call" idea — `processPendingJobs()`'s internal advance-check only fires when `jobsProcessedCount > 0` (`OrchestratorService.js:686-691`); the explicit call inside `runHourlyTrigger`'s `run('hourly')` path is the only coverage when nothing was PENDING that run.

**Verification:** pull a fresh SysLog export after a full real sync; confirm each real stage transition produces exactly one "State saved" row. Deliberately fire two concurrent `_checkAndAdvanceSyncState` invocations (editor, per §1.3's smoke pattern); confirm one proceeds, one logs `lock-contention`/idempotency no-op, state stays consistent. Re-run the 2026-06-14 regression scenario (`generateWebExportBackend` racing a concurrent `_checkAndAdvanceSyncState`); confirm `webExportFilename` survives.

**Review status: passed independent review 2026-08-26, 11th pass, no findings — ready to build.** Rounds 1-10 each found and closed a real gap (guard scope, completeness-gate blind spots, an invented-and-unimplemented return contract, a self-defeating timestamp fix, silent-clobber races in multi-write functions, catch-block disambiguation, lock-contention handling, an incomplete write-site enumeration, an object-mutation trap, a rule-polarity collision) — full trail above. Two narrow residual risks are explicitly accepted, not open: a latent blind spot if a future refactor wraps `mutateSyncStateBestEffort` in a shared helper (point 4), and a rare snapshot-loss case if a real write lands in `rebuildSysConfigFromSource`'s unlocked rewrite window during an active sync on a deploy day (point 5). Build order: Stage A first, 24h+ observation, then all of Stage B in one deploy per its own "why Stage B isn't split further" rationale above.

**Stages C/D/E** — unchanged from §1.3 (`purgeOldJobs`; `processPendingJobs` pick-up; `pullOrders` start), each its own 24h+ observation window — genuinely independent subsystems, so staging those still reduces risk.

**Status:** plan finalized 2026-08-26 (3rd pass), grounded in a full-repo write-site audit; passed independent review (11th pass) — ready to build.

## Bug 6: Validation rule ERRORs are silently treated as pass, letting unvalidated data reach product/order masters

**Found 2026-08-26** while investigating whether Comax vintage changes could go undetected. Two separate questions, two separate answers: if the whole Comax import fails (throws), the master is safe — `_upsertComaxData` is never reached. But if the `comax_staging` suite runs to completion and one rule inside it throws (missing sheet/map, bad row data), that specific rule is recorded as `status: 'ERROR'` by `ValidationLogic.runValidationSuite`'s per-rule try/catch (`ValidationLogic.js:459-485`) — and nothing downstream treats `ERROR` as a reason to block. The suite as a whole still reports success, so the master upsert proceeds as if every rule had genuinely passed.

**Three independent places make this same "block or not" decision, and all three only check for `'FAILED'`, never `'ERROR'`:**
1. `ValidationOrchestratorService.processValidationResults` (`ValidationOrchestratorService.js:29-54`) — feeds `comax_staging`, `web_staging`, `web_xlt_staging` (`ProductImportService.js:346,744,230`; also 5 more `web_staging`/`web_xlt_staging` sites in `WooProductPullService.js:226-227,247-248,335-336,357-358,429-430`, all routed through the same function so they inherit the fix automatically) and `master_master` (via `processJob`, line 85, and the separate report-only periodic check at `HousekeepingService.js:708`).
2. `OrderService.js` web-order import (`OrderService.js:164`) — an independent inline check, `validationResult.results.some(r => r.status === 'FAILED')`, doesn't go through `processValidationResults` at all.
3. `WooOrderPullService.js` Woo API order pull (`WooOrderPullService.js:61`) — the same independent inline check.

An `ERROR` result is worse than a `FAILED` one, not milder — a `FAILED` result at least tells you what's wrong; an `ERROR` means the check never ran at all, so there's no way to know if the data is safe. Failing closed is correct.

**Fix — widen all three checks, no new mechanism needed:**
- `processValidationResults`: add `else if (result.status === 'ERROR')` alongside the existing `'FAILED'` branch — increment `failureCount` and force `quarantineTriggered = true` unconditionally, regardless of the individual rule's own `on_failure_quarantine` flag (a rule that never ran has no severity to honor). No new notification call needed: `master_master`'s caller (`processJob`, `ValidationOrchestratorService.js:94-100`) already fires `NotificationService.reportFailure` on any `quarantineTriggered`, and the three `ProductImportService.js` staging paths already log a CRITICAL `SysLog` line and set the job to `QUARANTINED` on any `quarantineTriggered` — both become correctly non-silent for free once the flag is set correctly.
- `_buildQuarantineErrorMessage` (`ProductImportService.js:52-71`) currently filters only `status === 'FAILED'` when building the human-readable quarantine message, so an ERROR-only quarantine falls through to a generic "Validation failed - data quarantined." string with no indication of which rule errored or why. Widen the filter to `status === 'FAILED' || status === 'ERROR'` and format `ERROR` entries using `result.message` (the caught exception text) instead of a discrepancy count, so the operator sees the actual cause.
- **A second, separate filter needs the same widening (independent review, 2026-08-26):** each of the three `ProductImportService.js` quarantine blocks (`:230-234` web_xlt, `:346-350` comax, `:744-748` web) logs `'CRITICAL: Quarantine triggered - MASTER UPDATE BLOCKED'` with a `validationFailures` metadata field built from its *own* inline `validationResult.results.filter(r => r.status === 'FAILED')` — distinct from `_buildQuarantineErrorMessage`'s filter, and not fixed by widening that function alone. If a quarantine is triggered solely by an ERROR, this metadata field would silently be `[]` even though the message string (post-fix) correctly names the cause. Widen this filter too, in all three call sites, to `r.status === 'FAILED' || r.status === 'ERROR'`.
- **`ContactService.js#runCrmValidation` (`:1013`, its own inline `.filter(r => r.status === 'FAILED')` at `:1021`) has the identical blind spot but is explicitly out of scope** — it only shapes a report surfaced via `AdminContactsView.html:1546`'s manual "run CRM validation" button, gating no master write. Noted, not fixed, so it isn't mistaken for an unverified gap: the scope-check item above was actually run (independent review, 2026-08-26) and this is the one other consumer it found.
- `OrderService.js:164` and `WooOrderPullService.js:61`: widen the condition to `r.status === 'FAILED' || r.status === 'ERROR'`. Both already log/throw visibly once triggered — no new visibility code needed there either.

**Scope check — done (independent review, 2026-08-26):** grepped every `ValidationLogic.runValidationSuite(` call site repo-wide. All route through `processValidationResults` except the two already named (`OrderService.js`, `WooOrderPullService.js`) and `ContactService.js#runCrmValidation`, a report-only path noted above as out of scope. No other independent decision-maker exists.

**Status:** fix designed and independently reviewed (1 pass) — mechanism confirmed sound, two gaps found and closed above (the second `validationFailures` filter, the CRM-report scope note). Ready to build.

## Bugs 7+: dedicated audit findings, 2026-08-26

**Why this section exists.** After bugs 1-6 were designed/reviewed, the user asked directly whether anything else in the sync pipeline was broken. The honest answer was "unverified" — bugs 1-6 came from either a specific incident report or a question that happened to surface one, not from a systematic look. Five parallel agents then read every sync-adjacent file in full (state machine core, Comax import/validation, web/order pull pipeline, inventory push + housekeeping, widget frontend) hunting for defects independent of what was already known. Findings below are diagnosed, not yet designed into fixes or reviewed — same status bugs 1-3 were at in May. **None of these are built. None are reviewed. This is the raw material for the next design pass, not a completed plan.**

### Design-level gaps (the same underlying flaw repeats across sites — fix once, apply everywhere, not a per-site patch)

- **D1. No locking outside what Bug 5 introduces.** `OrchestratorService.js#processPendingJobs`/`processSessionJobs` (`:522-695`, `:1703-1809`) read-then-write `SysJobQueue` rows with no lock — the hourly trigger and a synchronous user-triggered call (e.g. `pushWebInventoryBackend`, `WebAppSync.js:533`) can both see the same row `PENDING` and both process it, double-pushing to WooCommerce. `ConfigService.setConfig` (`:228-267`) has the identical unlocked check-then-append shape — two near-simultaneous first-writes of the same new key create a permanent duplicate row. **Fix direction:** extend Bug 5's `withScriptLock`/`mutateSyncState` pattern to these two call sites rather than a separate mechanism.
- **D2. Sync-collision guard applied inconsistently.** `performFrequentMaintenance` correctly checks for an active sync before its destructive writes (`HousekeepingService.js:592-603`) — its sibling `performDailyMaintenance` does not, and does full read→clear→rewrite passes over `WebOrdM`/`SysJobQueue`/`SysTasks` that can silently roll back a concurrent sync's writes (`:666-821`, e.g. `archiveCompletedOrders` at `:1681-1902`). `pullBundleProducts` (`WooProductPullService.js:569-618`) does the same unguarded full-sheet rewrite its sibling `refreshTranslationLinks` explicitly guards against (`:406-416`), live behind an admin button with no warning. **Fix direction:** one shared guard helper, audited onto every full-sheet clear-and-rewrite site — this is the same class of gap §1.5 already flags for `WebXltM`/`CmxProdM`/`SysProductAudit`; these two are more instances of it, not a new category.
- **D3. `RUNTIME_KEYS` (the config-rebuild preservation list, Bug 5 §5) is hand-maintained and already stale.** At least 10 runtime-written `SysConfig` keys exist with no entry in it — `crm.pending_payment_followup.sent_order_ids`/`.floor_date` among them (`HousekeepingService.js:1164-1167,1312-1314`). A routine `rebuildSysConfigFromSource()` (the documented config-change deploy step, not an emergency action) deletes these outright. Concretely: wiping the payment-followup dedup state makes the next run re-seed from scratch with no memory of who's already been emailed, and `GmailApp.sendEmail` (`HousekeepingService.js:1267`) fires real duplicate follow-ups to real customers, no error, no log flag. **Fix direction:** don't just add the missing 10 keys — that recreates the same drift the next time someone adds a runtime-written key. Needs either auto-discovery (grep-verified at build time, like Bug 5's completeness gates) or a registration requirement enforced some other way.
- **D4. The widget's action-safety net (`runAction`'s 2-minute timeout, its stale-poll discard) was never applied to its sibling action functions.** `retryFailed()`/`resetSync()` (`AdminDailySyncWidget_v2.html:514-564`) set `actionInProgress = true` like `runAction` but arm no timeout — if the backend call hangs, `actionInProgress` never clears and every future click is silently blocked with no recovery short of a page reload. Neither updates `lastActionTimestamp` on success either, so a stale in-flight poll can revert a just-completed retry/reset back to the old (failed/complete) display. **Fix direction:** these are two near-duplicate reimplementations of `runAction`'s lifecycle that drifted — extract one shared helper instead of patching each copy again.

### Individual bugs (self-contained, no redesign needed)

- **I1. `wps_TaxProductBrand` reads the wrong field on every EN product pull.** `WooProductPullService.js:164-166,512-515` routes brand through `_extractNames` (built for term objects) instead of `_getAttributeValue` (used correctly for every other attribute) — every pull writes the literal attribute label `"Brand"` instead of the actual winery/producer name. Live, ongoing, no error. Highest-priority individual fix.
- **I2. Order line items with a blank SKU are dropped with zero logging.** `OrderService.js:857-862` — the sibling "SKU lookup failed" branch two lines below logs an error; this one doesn't. Inventory deduction and Comax export both silently miss that unit while the order total still reflects full payment.
- **I3. A known-broken WPML lookup is still live in an alternate code path.** `WooProductPullService.js:198` (`_extractAndStageTranslationLinks`, reachable via `pullWooProductsBackend`) still does the exact lookup the same file's own comment at `:451` says was fixed and replaced elsewhere (`_transformApiTranslation`). The fix was never backported to this second path.
- **I4. Boolean vs. string `TRUE`/`FALSE` mismatch silently disables 3 CRM audit rules.** `ValidationLogic.js#evaluateCondition`'s `=`/`<>` branches (`:19-20`) compare the raw value, not the lowercased/trimmed one every other operator uses. `sc_IsCore` is a real JS boolean (`ContactImportService.js`); `String(true) !== 'TRUE'`, so `validation.crm.noncore_core_mismatch`/`iscore_prospect_mismatch`/`notcore_core_mismatch` can never fire. The codebase already has the right pattern elsewhere (`ContactService.js:933`, `KPISummaryService.js#_isTrue`) — `evaluateCondition` just never got it.
- **I5. A validation summary task's hardcoded `entityId: 'SYSTEM'` permanently breaks its own dedup mechanism.** `ValidationOrchestratorService.js:140-149` — once a rule creates a summary task (>10 discrepancies), the `skip_if_open_task_type` check (keyed by real entity IDs) can never match it, so every lower-priority rule that's supposed to defer to it instead creates duplicate tasks every single validation cycle until the summary task is manually closed.
- **I6. `_upsertWebProductsData` resolves the data spreadsheet by Drive file name, not ID.** `ProductImportService.js:943` — `DriveApp.getFilesByName('JLMops_Data').next()` instead of the fixed-ID `SheetAccessor.getDataSpreadsheet()` every other write path uses. A second file sharing that exact name (a backup copy, a test duplicate) could silently redirect a live product-master write to the wrong file.
- **I7. Comax rows with a blank `cps_CmxId` are silently dropped, with no logging** (`ProductImportService.js:395-397`) — the identical condition IS logged two lines later in `_maintainSysProductAudit` (`:573-577`) on the same data, so the gap is inconsistency, not a missing capability.
- **I8. `WooInventoryPushService._runPush` pushes malformed CSV values to live WooCommerce as a reported success.** `WooInventoryPushService.js:134-137` — a blank stock cell becomes `stock_quantity: 0`; a blank price cell becomes `regular_price: ""`, which WooCommerce treats as "clear the price." Both silently succeed with no SKU flagged anywhere.
- **I9. `isSessionStale` (`SyncStateService.js:228-238`) is fully implemented and has zero callers.** No recovery path exists for a sync stuck in a synchronous `*ING_*` stage (`IMPORTING_PRODUCTS`/`IMPORTING_ORDERS`/`EXPORTING_ORDERS`) killed by Apps Script's 6-minute execution limit mid-call — `_checkAndAdvanceSyncState` only has recovery branches for the three async-job stages. This is the same "stuck-stage detector" cross-cutting item already spec'd (never built) in this doc's May section — confirmation it's a real gap, not new scope.
- **I10. `resetSyncStateBackend` drops deferred file-registry entries.** A step that completed successfully before Reset was clicked has its `SysFileRegistry` write silently lost (`archiveFileIds` is never flushed before the state is overwritten), causing the next sync to re-process a file that already succeeded.
- **I11. The post-sync bundle-health trigger is unreachable.** Only `SyncStateService.transition()` schedules it, and every real COMPLETE-setting code path bypasses `transition()` in favor of a direct `setSyncState()` call. Soft impact — `checkBundleHealth` also runs via daily maintenance — but the "right after sync" mechanism as designed never fires.
- **I12. Two low-priority/dormant items, not worth independent design effort:** `skipToComaxBackend` (`WebAppSync.js:901-919`) writes stale `ordersPendingExportCount`/nulled step data — currently unreachable (no UI caller). `config/jobs.json` still declares a `depends_on` the code comments claim was removed (`generate-config.js` / `OrchestratorService.js:363-365`) — harmless today since the state machine's own stage-gating already enforces order, but inconsistent.

**Individual-bug status, 2026-08-26:** I1, I2, I3, I4, I5, I6, I7, I8, I10 fixed in the working tree (not deployed, not committed — see git status). I9 and I11 deferred to Bug 5's build, since both touch write paths Bug 5 is already migrating and a standalone patch now would just be redone. I12 left as-is (dormant, no live path).

### D1: No locking outside Bug 5's fix — job-queue pick-up and `ConfigService.setConfig`

**Load-bearing constraint, stated once here because it governs both fixes below and every future one (independent review, 2026-08-26, found the first draft violated it):** `withScriptLock` wraps `LockService.getScriptLock()` — **one single script-wide mutex, not a set of independently-named locks.** The `context` string passed to it is a log label only; GAS has no keyed/named locking primitive. Any code that acquires this lock must never call anything that tries to acquire it again in the same call stack — the lock is not reentrant, so a nested acquisition attempt hangs until its own timeout and then throws. This is why the first draft of the `setConfig` fix below was wrong, and it constrains how any future locked code in this codebase can be composed.

**`processPendingJobs`** (`OrchestratorService.js:606-684`) and **`processSessionJobs`** (`:1703-1809`) both read `SysJobQueue` for a `PENDING` row, then separately write `PROCESSING` to it — unlocked, and both target the same sheet, so they can race against each other, not just against themselves. This is `RELIABILITY_AUDIT.md` §1.3 Stage D's scope, just never built. The zombie-killer sweep earlier in `processPendingJobs` (`:566-598`) has the same unlocked read-then-write shape.

**Enumerating `SysJobQueue` writers by hand has now failed twice in a row, the same failure mode Bug 5's write-site audit hit three times before switching to a governing rule instead of a list (2nd and 3rd independent review, 2026-08-26).** Beyond the pick-up/claim loops and the zombie sweep, confirmed live and unlocked: `finalizeJobCompletion`'s Phase 2 unblock step (`:925-950`, called from 3 sites); **`_reapStuckJobInSession`** (`:1102-1167`) — a *separate* function from the `processPendingJobs` zombie sweep, added by the 2026-05-05 Bug 4 fix, doing the identical scan-and-mark-`FAILED` pattern, called from all three `_checkAndAdvanceSyncState` branches at the same 1-second polling cadence that produced Bug 5's 5-12-concurrent-execution bursts; and **each job-processor service's own terminal-status write** — `ProductImportService._updateJobStatus`, `OrderService._updateJobStatus`, `WooInventoryPushService._updateJobStatus`, `ValidationOrchestratorService._updateJobStatus` — every one unconditional and unlocked, and every one capable of racing a reaper (a job that's actually still running writes `COMPLETED` at the same moment a reaper marks the same row `FAILED`; last write wins, no coordination).

**A fourth writer, dead but real, missed by the "confirmed" list (4th independent review, 2026-08-26):** `WebAppSystem.js:75-77` does the identical raw unlocked write inside a catch block — zero callers anywhere (confirmed via grep, including `.html`), dead code today like `skipToComaxBackend`/`updateSyncStateFromOrchestrator` elsewhere in this doc, but never previously named. Migrated along with everything else so it isn't a live trap if ever wired up.

**A fifth writer, also dead, missed again (6th independent review, 2026-08-26 — proof the "honest grep, human-checked" completeness bar from the last round hadn't actually been fully applied yet):** `WebApp.js:315-349`, `runDailyCriticalValidations()`, does `jobQueueSheet.appendRow(rowValues)` (`:334`). Zero external callers (confirmed via grep), and it delegates to `ValidationService.runCriticalValidations` — a function that doesn't exist anywhere in the codebase (only referenced in `docs/ARCHITECTURE.md` prose), so this function would throw a `ReferenceError` if ever invoked, caught by its own try/catch before any further write. Doubly dead. Unlike the other four sites, this is an insert-only `appendRow`, not a status-column update, so it doesn't need `setJobRowStatus`'s guard semantics — noted here so it's on the record rather than silently absent, consistent with `WebAppSystem.js`'s treatment above.

**Fix — two centralized locked primitives, reusing Bug 5's already-proven contract shape rather than inventing a new one.** Add to `OrchestratorService.js`, the file that already owns job-queue mechanics:
1. **`_claimNextPendingJob(jobQueueSheet, jobQueueHeaders, filterFn)`** — locked, re-reads fresh, finds the next row matching `filterFn` (all jobs for `processPendingJobs`, a specific `sessionId` for `processSessionJobs`) and marks it `PROCESSING`. **Return contract, three outcomes not two (5th independent review, 2026-08-26: two outcomes conflated "genuinely no jobs left" with "lock was contended, unknown," which matters for `processSessionJobs`'s stop-on-first-failure contract — a contended claim could otherwise be misread as "done"):** `{claimed: true, jobId, jobType, jobQueueSheetRowNumber}` on success; `{claimed: false, contended: false}` if a fresh scan genuinely found nothing matching; `{claimed: false, contended: true}` if the lock itself couldn't be acquired. **Retry bound made explicit, not "a few times" (6th independent review, 2026-08-26: unspecified retries risked unbounded blocking exactly in the high-contention 1s-polling scenario this fix targets, and diverged from Bug 5's own precedent — `mutateSyncState` throws immediately on contention rather than auto-retrying internally, pushing the retry decision to the caller).** `_claimNextPendingJob` itself uses a short per-attempt lock timeout (5s, not `mutateSyncState`'s 30s default — this is a hot, frequently-contended path, fail fast) and reports `contended: true` on the first failed attempt rather than looping internally. The caller retries at most 2 additional times, ~3s apart (matching the existing `WooInventoryPushService` auto-retry precedent already in this codebase, Bug 4's fix log), then treats persistent contention as "nothing claimable this run" — for `processPendingJobs`, the outer loop/trigger cadence covers the next attempt; for `processSessionJobs`, this surfaces as a normal job-processing failure through its existing stop-on-first-failure path, not a silent "done."
2. **`setJobRowStatus(jobQueueSheet, jobQueueHeaders, jobId, fn)`** — **keyed by `job_id`, not row number (7th independent review, 2026-08-26 — see the `purgeOldJobs` finding below for why a row number isn't safe to hold across the unlocked work window).** Takes a mutator callback, not a static `updates` object (4th review: the static-object version serialized writes without stopping them clobbering each other — two reapers, or a reaper and a real completion, would each still unconditionally overwrite the row in turn, just one after another instead of concurrently). Locked, re-reads the sheet fresh, finds the row currently matching `jobId` (not a remembered row number), calls `fn(currentRowValues)` — `fn` returns the fields to write, or nothing/`undefined` to signal "don't apply, my precondition no longer holds" (including "row not found," treated the same as any other failed guard), exactly mirroring `mutateSyncState`'s `fn` contract. **`fn`'s guard is per-site, not one shared condition (5th review: the single "only if still PROCESSING" description was wrong for the one site not transitioning out of PROCESSING — see Phase 2 below).** Two variants, mirroring `mutateSyncState`/`mutateSyncStateBestEffort`:
   - **`setJobRowStatus` (must-apply):** for a job processor's own terminal write (real work already happened — a WooCommerce push, an import — so losing this write silently would misreport a job that actually succeeded, the same symptom class Bug 4 already fixed once). Same explicit retry bound as `_claimNextPendingJob` above (5s per-attempt timeout, up to 2 retries ~3s apart) before giving up; throws if still contended after that. **The throw must actually surface, not just avoid crashing anything (5th review found a real gap here — see below).** `fn`'s guard: only write if current status is still `PROCESSING`.
   - **`setJobRowStatusBestEffort`:** for the zombie sweep and `_reapStuckJobInSession` — opportunistic sweeps where "someone else already handled this row" is a normal, expected outcome. Silently no-applies on contention or a failed guard; `fn`'s guard: only mark `FAILED` if status is still `PROCESSING`, so a reaper can never overwrite a real, already-recorded `COMPLETED`. **`finalizeJobCompletion`'s Phase 2 unblock uses this same variant but a DIFFERENT guard** — it writes `BLOCKED → PENDING`, never touches `PROCESSING`/`FAILED` at all, so `fn` here checks "is status still `BLOCKED`," not the zombie-sweep guard above. **Confirmed low-priority in practice, precisely stated (6th independent review, 2026-08-26: the prior wording overclaimed):** grepped the entire codebase for the literal string `'BLOCKED'` — it appears in exactly one place, the read check inside Phase 2 itself (`OrchestratorService.js:935`); no `createJob` call site or anything else in the JS source ever writes that status. That means no *code path* currently produces a `BLOCKED` row — it does not rule out a manually-edited sheet cell or a stale row from a since-removed mechanism, so "can never occur" would overstate it. Phase 2's unblock logic is dead-by-code-path today (consistent with `.claude/bugs.md`'s existing note that `depends_on` enforcement was never actually wired up) — migrate it for consistency; the `BLOCKED`-check guard handles a real row correctly regardless of how it got there, so this precision doesn't change the fix, only the confidence claim around it.

This is exactly the "does this write change the thing I depend on" governing rule Bug 5's guard design already established — reused here for job rows instead of `system.sync.state`, not a new design.

**A real gap in how the must-apply throw actually reaches anyone (5th independent review, 2026-08-26) — the "propagates into the existing catch block, no new risk" framing was true but answered the wrong question.** Traced two real callers: `ProductImportService.js:1079-1082` and `OrderService.js:177-180` both call `_updateJobStatus(executionContext, finalJobStatus)` and then, **unconditionally on the local `finalJobStatus` variable** (not on whether the write actually landed), proceed to call `OrchestratorService.finalizeJobCompletion(...)` if it's `'COMPLETED'`. But `_updateJobStatus`'s own existing try/catch (`ProductImportService.js:22-44` and equivalent in the other three services) only logs and returns — it never rethrows. So a must-apply throw from `setJobRowStatus` lands in that catch, gets logged, and the caller proceeds exactly as if the write succeeded: logs success, calls `finalizeJobCompletion`, advances state — while the real `SysJobQueue` row is stuck at `PROCESSING` forever, a live target for the zombie sweep to then mark a genuinely-completed job `FAILED`. This reproduces the exact misreporting this whole fix exists to prevent, one call frame removed.

**Fix:** each service's `_updateJobStatus` must return whether the write actually applied (a boolean, or `{applied}` matching Bug 5's shape) instead of nothing — its internal catch block still logs, but returns `false` rather than swallowing into an implicit "success." Every caller that currently proceeds unconditionally after calling `_updateJobStatus` must check that return value and only proceed with success-path logic when it's `true` — surfacing a write failure visibly (a warning log, or `NotificationService.reportFailure` matching Bug 6's precedent) rather than silently pretending the row was updated. **Confirmed which callers actually need which gate (6th & 7th independent review, 2026-08-26, traced all four services):** `ProductImportService.js:1079-1082` and `OrderService.js:180` gate a real downstream action, `OrchestratorService.finalizeJobCompletion(...)`, on the local status variable — these need the full fix. `WooInventoryPushService.js`'s caller has an unconditional post-write success log line (`:50`) that needs the same gating. **`ValidationOrchestratorService.js` needs no caller-side change at all (7th review corrected the prior round's claim here)** — its `processJob` (`:67-126`) has no unconditional action of any kind after the `_updateJobStatus` call at line 120, nothing to gate; the return-value change is still worth making on the primitive itself for consistency, it just has no caller to update in this one file.

Both primitives share the one script-wide lock with each other and with Bug 5's sync-state writes, by construction. The slow part — service delegation (`:651-671`) — stays outside the lock, per §1.3's own warning; the "no processing service configured" branch (`:624-632`, decided before the claim) fits by claiming the row unconditionally first, then failing it back out via `setJobRowStatus` if `jobConfig` turns out invalid.

**Two more findings, 7th independent review, 2026-08-26:**

**(a) `_checkAndAdvanceSyncState`'s IMPORTING_COMAX branch calling `processPendingJobs()` unconditionally (`OrchestratorService.js:1202`) looked like a fresh exposure to Bug 5's 5-12-concurrent-execution bursts — it isn't, once Bug 5's own already-designed fix is accounted for.** Bug 5's guard rule (above) already requires every side effect in `_checkAndAdvanceSyncState`'s three branches — explicitly including this exact `finalizeSync`/`processPendingJobs()` pair — to fire only when that execution's write has `applied: true`. So only the ONE execution that wins the idempotency race reaches this call; the other 4-11 concurrent executions no-op before ever calling `processPendingJobs()`. No new design needed here — just correctly citing that this is already closed, not leaving it looking unaddressed.

**(b) `finalizeJobCompletion`'s Phase 3 (`OrchestratorService.js:955`) calls `processPendingJobs()` recursively from inside a job it dispatched — genuine, pre-existing recursion, not introduced by D1.** **Correction, 8th independent review, 2026-08-26: "bounded by monotonically shrinking pending-job count" is not the real mechanism.** `processPendingJobs()` itself calls `_checkAndAdvanceSyncState()` when it processed anything (`:691`), whose IMPORTING_COMAX branch can call `finalizeSync` → `createJob` (`:388`) → queue a **new** `PENDING` validation job → call `processPendingJobs()` again — so completing one job can add another to the pool, not just remove one; the pool isn't monotonically shrinking. The recursion still terminates in practice, but for a different reason: each stage that creates a follow-on job (validation-after-Comax, etc.) is one of a small, fixed set of sync-flow steps, each gated by its own "already pending/processing" idempotency check that stops it re-queuing the same follow-on job indefinitely — not by raw job-count arithmetic running out. **Accepted, not fixed here** either way: redesigning `processPendingJobs`'s recursive-dispatch structure is a separate, pre-existing architectural question outside a locking fix's scope — D1 only adds a bounded ≤21s-worst-case retry cost per claim attempt on top of whatever this recursion already costs today.

**(c) `HousekeepingService.js#purgeOldJobs` (`:2144-2146`) does a whole-sheet `clearContents()` + `setValues()` rewrite of `SysJobQueue`, completely unlocked — this is `RELIABILITY_AUDIT.md` §1.3's own Stage C, deliberately sequenced after Stage D (this fix) with its own 24h+ observation window, not scope creep into D1.** It creates a real row-shift hazard for anything holding a job's row number across the deliberately-unlocked "real work" window — closed for `setJobRowStatus` by keying it on `job_id` instead (above).

**A second, row-number-keyed consumer on the identical call path was missed by that fix (8th independent review, 2026-08-26) — `finalizeJobCompletion` itself.** `finalizeJobCompletion(jobQueueSheetRowNumber)` (`OrchestratorService.js:858`) takes a row number as its entire identity and does an unlocked `getJobDetailsByRow` lookup (`:871`, `:986-996`) by that number — the same stale-row-number exposure `setJobRowStatus` was just fixed to avoid, one function downstream, on the exact same call path: all three real callers (`ProductImportService.js:1082`, `OrderService.js:180`, `processSessionJobs` at `:1779`) capture `jobQueueSheetRowNumber` before the unlocked real-work window runs, then pass that same original number into `finalizeJobCompletion` afterward. If `purgeOldJobs` shifts/removes the row during that window, this lookup either silently no-ops (skipping Phases 1-4 for a job that genuinely completed) or, worse, reads whichever different job now occupies that row number and misattributes file-registry writes, dependency-unblocking, and state-triggers to the wrong job. **Fix: re-key `finalizeJobCompletion` the same way — take `jobId` (already available in every caller's `executionContext`) instead of a row number, and do a fresh `job_id`-based lookup for `completedJobType`/`completedJobSessionId` at call time.** Read-only, so this doesn't need the lock itself — just the same "don't trust a number captured before unlocked work happened" principle already applied to the write side. Stage C's own future locking (deferred per §1.3) closes the *loss-of-enqueued-work* problem `purgeOldJobs` was flagged for; job_id-keying here and in `setJobRowStatus` independently closes the *stale-row-number* problem for everything that reads or writes a specific job across that window, without needing to pull Stage C forward.

**Build-order note, made explicit (8th independent review, 2026-08-26: implied by document order, never stated as a hard prerequisite):** D1 reuses `withScriptLock` and the `mutateSyncState`/`mutateSyncStateBestEffort` contract shape directly — none of which exist in the codebase yet (grep-confirmed zero hits for all three). D1 cannot build before Bug 5's Stage A (the lock helper) ships; the applied-gating claim in point (a) above specifically requires Bug 5's Stage B to have shipped too, not just be designed. D1 is downstream of Bug 5, not parallel to it.

**Completeness check — corrected again, honestly this time, after the "structurally-guaranteed anchor" from the last pass turned out not to be one (5th independent review, 2026-08-26).** The `getSheetByName(sheetNames.SysJobQueue)` grep missed most real acquisition sites: `processPendingJobs` itself uses an intermediate variable (`getSheetByName(jobQueueSheetName)`, `OrchestratorService.js:535,545`); `ProductImportService.js:25` uses inline property access (`getSheetByName(allConfig['system.sheet_names'].SysJobQueue)`); and `OrderService.js`, `WooInventoryPushService.js`, `ValidationOrchestratorService.js`, `WebAppSystem.js` all go through a `SheetAccessor.getLogSheet(...)` wrapper whose own internal `getSheetByName` call is textually disconnected from the string `SysJobQueue`. There is no single syntactic form to anchor a purely mechanical, judgment-free grep on — unlike Bug 5's `mutateSyncStateBestEffort(` check, which worked specifically because it targeted a brand-new function name with no legacy call-site variety. **Honest version:** `grep -rn "SysJobQueue" jlmops/*.js` — broader, catches every idiom (all three forms above do contain the literal string somewhere in the call), but not self-verifying; each hit needs a human to check whether it's a write path and whether that path resolves into one of the three primitives. The actual completeness claim for this fix rests on the audit trail above — five independent review rounds tracing real call chains, not on any single grep — and that should be stated plainly rather than implied to be more mechanical than it is.

**`processSessionJobs`'s loop shape needs restructuring, and must NOT borrow `processPendingJobs`'s continue-after-failure behavior along with its shape (4th review found the first draft's "matching shape" language would have silently changed this too).** Unlike `processPendingJobs`'s while-loop (catches a job failure, marks it `FAILED`, and continues the loop to process further `PENDING` jobs, `:673-681`), `processSessionJobs` currently returns/aborts immediately on the first job failure (`:1740-1745`, `:1786-1803`) — a deliberate, different contract, since this function processes one session's own jobs synchronously for a single `*Backend` call, which needs to stop and report back to its caller on the first problem, not keep working through unrelated jobs the way the background hourly sweep should. Restructure the claim step alone into a while-loop that calls `_claimNextPendingJob` repeatedly (fitting its "re-read fresh inside the lock" contract) — but keep the existing stop-immediately-on-failure control flow around it unchanged. Only the claim mechanism is shared with `processPendingJobs`; the loop-continuation policy is not.

**`ConfigService.setConfig`** (`:228-267`) has the same unlocked check-then-write shape, general-purpose across every config key — not just `system.sync.state`. **The first draft wrapped `setConfig`'s own body in `withScriptLock` — this is wrong and would have broken Bug 5 outright**, not just D1: `SyncStateService.setSyncState` (`SyncStateService.js:97`) calls `ConfigService.setConfig` unconditionally, and Bug 5's `mutateSyncState`/`mutateSyncStateBestEffort` already hold the lock for the whole read-modify-write span that includes this call. Every single migrated sync-state write would try to re-acquire the lock it's already holding — hang for 30s, then throw, on every write, the moment D1 shipped.

**Corrected fix — two entry points, not one locked function:** leave `ConfigService.setConfig` itself unlocked (its current callers that are already inside a lock keep calling this raw version, unchanged). Add a new `ConfigService.setConfigLocked(settingName, key, value)` that wraps a call to the existing unlocked `setConfig` in `withScriptLock`, and migrate every general-purpose call site to call `setConfigLocked` instead. On contention, `setConfigLocked` throws a clear "could not acquire SysConfig lock" error — `setConfig` has dozens of callers with no shared expectation of a silent "write skipped" outcome, so failing loudly is the safer default, matching `mutateSyncState`'s write-1 contention behavior in Bug 5.

**A second already-locked caller, missed in the first draft (2nd independent review, 2026-08-26): `SetupConfig.js`'s `rebuildSysConfigFromSource` (templated in `generate-config.js`, both confirmed identical at the relevant line).** Bug 5 §5's finalized fix for this exact function requires its `RUNTIME_KEYS` restore loop's `ConfigService.setConfig(rk.name, rk.key, value)` call (`SetupConfig.js:91`) to run *inside* a lock hold that also covers the preceding compare-read ("the compare and the restore write happen inside the same lock hold, never released between them" — Bug 5 §5, above). A naive "migrate every general call site to `setConfigLocked`" sweep would catch this line too, since nothing in the code distinguishes it from an ordinary call site today — nesting `setConfigLocked`'s own lock acquisition inside Bug 5's already-held one, hanging then throwing on every `rebuildSysConfigFromSource()` run, i.e. every routine config deploy. **This call site must be explicitly excluded from the migration and keep calling the raw unlocked `setConfig`, exactly like `SyncStateService.setSyncState`** — both are noted here together so a future pass has one place to check, not two easy-to-miss exceptions.

### D2: Sync-collision guard applied inconsistently

`performFrequentMaintenance` checks `SyncStateService.getActiveSession()` before its destructive writes (`HousekeepingService.js:592-603`); its sibling `performDailyMaintenance` does not, despite `archiveCompletedOrders` (`:1681-1902`), `purgeOldJobs` (`:2078-2157`), and `archiveCompletedTasks` all doing the same read-all→clear→rewrite pattern on sheets a sync can be actively writing to. `WooProductPullService.pullBundleProducts` (`:569-618`) does the identical unguarded full-sheet rewrite on `WebProdM`/`WebXltM`. This is the same defect class `RELIABILITY_AUDIT.md` §1.5 already scopes for `WebXltM`/`CmxProdM`/`SysProductAudit`'s *write-time* guard (don't write empty over populated) — D2 is the complementary *entry* guard (don't even start the destructive read-modify-write while a sync is active), and both should ship together since they defend against the same underlying collision from opposite ends.

**Correction (independent review, 2026-08-26): the third "already correctly guarded" sibling this section originally cited as a working example was itself broken and has now been fixed directly.** `WooProductPullService.refreshTranslationLinks` (`:406-417`) checked `syncState.currentStage` — but `SyncStateService`'s state object has a `.stage` field, never `.currentStage`, so the condition was always `undefined` and the guard never fired, for as long as this code has existed. Fixed (see git diff, `:408-410`) — this wasn't a copy-worthy precedent, it was a third live gap the original audit pass missed.

**Fix — two parts, since a naive "copy the same check" is wrong for `performDailyMaintenance` specifically (independent review, 2026-08-26):**
1. Extract `performFrequentMaintenance`'s existing check into a shared `_isSyncActive()` helper.
2. **`pullBundleProducts`** gets the straightforward version — call `_isSyncActive()` once at the top, before `_upsertBundleFieldsToMaster` runs, mirroring `performFrequentMaintenance`'s own whole-function-gate shape (the function does one thing).
3. **`performDailyMaintenance` must NOT be gated as a whole function** — confirmed on full read (`:666-821`) it runs ~20 unrelated subtasks (unit tests, schema validation, Mailchimp/coupon pulls, CRM intelligence, KPI recompute) alongside the three destructive ones. A whole-function gate would silently skip an entire day's worth of unrelated maintenance — tests, CRM, KPI — just because a sync happened to be active at trigger time, a materially worse failure mode than the guard is meant to prevent. Scope the check to wrap only `archiveCompletedOrders`, `purgeOldJobs`, and `archiveCompletedTasks` individually (or the block containing just those three), leaving the other ~17 subtasks unaffected by sync state.

§1.5's own scope line ("audited across all masters that use it, not just `WebXltM`") already commits to enumerating every clear-and-rewrite site — extend that same audit pass to confirm every one of them gets this entry guard, not just the sites found so far, so a fifth undiscovered gap isn't left exposed the same way these three were.

### D3: `RUNTIME_KEYS` (Bug 5, point 5) is hand-maintained and already stale — needs a build-time completeness check, not a bigger list

Adding the 10 currently-missing keys (`crm.pending_payment_followup.sent_order_ids`/`.floor_date`, `crm.frequent_pipeline.last_modified_floor`, `system.crm.welcome_floor_date`, `system.woocommerce.coupons_last_update`, `system.product_costs.last_recompute`, and 4 lower-severity cache keys) fixes today's drift but not the next one — nothing currently stops a future `ConfigService.setConfig` call site from writing a new runtime key without anyone remembering to register it. A **generic "preserve anything that differs from the master default" rule was considered and rejected**: `rebuildSysConfigFromSource`'s whole purpose is to make the live sheet match `config/*.json` exactly (its own docstring says so), so any row a `RUNTIME_KEYS`-style list doesn't cover is *supposed* to be wiped back to its shipped default on rebuild — a manual/stray edit to ordinary business config should not survive a rebuild. Preserving "anything that differs" would silently start keeping those stray edits too, a real behavior change beyond this bug.

**Fix — the same pattern as Bug 5's own completeness gates, applied here (confirmed workable, independent review 2026-08-26):** `generate-config.js` is plain Node (`require('fs')`/`require('path')`) with full filesystem access from the `jlmops` folder, and already builds the exact `(settingName, key, defaultValue)` data the check needs while generating `masterConfig` — grep the sibling `.js` source at generation time for every literal-string `ConfigService.setConfig(` **and, once D1 ships, `ConfigService.setConfigLocked(`** call site, extract each `(settingName, key)` pair, and fail the generation loudly if any resolved pair is covered by **neither** `RUNTIME_KEYS` **nor** a real (non-blank-placeholder) default already defined in `masterConfig`. Verified empirically against all 28 current call sites and all blank-default rows in `config/*.json`: every live case resolves correctly today, no false negatives.

**Two honest limitations, not fixed, stated so they aren't mistaken for covered (independent review, 2026-08-26):**
- **The literal-string blind spot is already live, not hypothetical.** `SyncStateService.js:97` calls `ConfigService.setConfig(SYNC_STATE_CONFIG_KEY, 'json', ...)` using a `const` variable for `settingName` — a naive grep/regex extraction can't resolve it. It happens to already be covered by `RUNTIME_KEYS` today (`system.sync.state` is in the list), so there's no live gap yet, but the check would silently pass this call site without actually verifying it, at the single highest-stakes call site in the whole plan.
- **Nothing enforces the invariant going forward.** A future setting shipped with a real non-blank default that later, in some other change, starts being written via `setConfig`/`setConfigLocked` at runtime would pass the check as "safe" (it has a real default) while actually needing `RUNTIME_KEYS` registration — the check catches missing keys, not a key silently changing category.

### D4: Widget's `retryFailed()`/`resetSync()` are near-duplicates of `runAction()` that drifted — missing its safety net, not differently designed

`runAction` (`AdminDailySyncWidget_v2.html:377-441`) arms a 2-minute safety timeout that force-clears `actionInProgress` and refreshes if the backend call never returns, and sets `lastActionTimestamp = status.lastUpdated` on success so a stale in-flight poll can't overwrite a fresh result. `retryFailed()` (`:514-534`) and `resetSync()` (`:536-564`) copy `runAction`'s `actionInProgress` guard and button-disable logic but never got either protection — confirmed on re-read, neither function references a timeout or `lastActionTimestamp` anywhere. Left as-is: a hung `retryFailedStepBackend()`/`resetSyncStateBackend()` call permanently blocks every future action (page reload is the only recovery), and a stale poll can revert a successful retry/reset back to the pre-action display.

**Fix — do not refactor `runAction` itself** (it already works, and it carries extra logic — step-card highlighting, business-level success/failure detection for the progress log — that doesn't apply to retry/reset and isn't worth the regression risk of touching). Instead, extract a small shared helper used only by `retryFailed`/`resetSync`, the two functions that are actually near-identical to each other: `_runManagedAction(rpcCall, {footerBusyText, footerDoneText, extraOnSuccess})` that owns the `actionInProgress` guard, button disable, arming/clearing a 2-minute timeout (mirroring `runAction`'s), and a standard success handler (`updateUI`, `adjustPolling`, `lastActionTimestamp = status.lastUpdated`, then `extraOnSuccess?.(status)`) and failure handler (reset `actionInProgress`, render the same Refresh-button fallback, prefix the error message). `retryFailed`/`resetSync` become thin callers passing their RPC name and the small bits that differ (reset's confirm dialog and progress-log clearing stay in `resetSync` itself, wrapping the call to the shared helper).

**Honest scope note (independent review, 2026-08-26):** this is a partial closure of the underlying defect class, not a full one — `runAction` still carries its own hand-maintained copy of the same timeout/`lastActionTimestamp` logic, so a future change to that shared contract (timeout duration, what counts as "done") still has to be updated in two places by hand, the same drift mechanism that produced this bug. Accepted tradeoff, stated explicitly rather than implied as fully solved: unifying all three into one helper would mean touching `runAction`'s step-card and business-success-detection logic too, which is the regression risk this fix deliberately avoids.

**Not yet done:** independent review for D1-D4, then build. Same standard as bugs 4-6 — designed here, not yet reviewed or built.

## Files in scope

- `jlmops/AdminDailySyncWidget_v2.html` — widget
- `jlmops/SyncStateService.js` — state machine + transition table (reference only)
- `jlmops/WebAppSync.js` — backend entry points (`getSyncStateFromBackend`, all `*Backend` functions)
- `jlmops/OrchestratorService.js` — async job advancement (`_checkAndAdvanceSyncState`, job status tracking)
