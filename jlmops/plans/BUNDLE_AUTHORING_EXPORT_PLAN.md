# Bundle Authoring + Export to WooCommerce — SUPERSEDED

**Superseded 2026-06-04 by [`BUNDLE_PLAN.md`](BUNDLE_PLAN.md)** (consolidated bundle master plan).

The authoring + export workflow (serializer, export table, `sb_PendingExport` / website-truth model, bundles-only direct-to-master pull, view load control, and the margin/diversity longer-term considerations) is now **Stages 1–2 + 4–5** of `BUNDLE_PLAN.md`.
